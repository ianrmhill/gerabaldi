# Link the base Gerabaldi dependencies
-r requirements.txt

# Required for testing the package
pytest >= 7.1.2
# Required for optional probabilistic programming tie-ins
pymc >= 4.1.4
pyro-ppl >= 1.8.4
torch >= 1.13.1
# Silly workaround for Python 3.7 requiring older pymc that requires older setuptools
setuptools < 50 ; python_version <= "3.7"
# Required for the optional demos
click >= 8.1.3
matplotlib >= 3.5.3
seaborn >= 0.12.2
reliability >= 0.8.6
